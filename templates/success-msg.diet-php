!extends 'layout.php'

block innerContent {
  <div class="container">
    <?= $header ? ('<h2 class="section_header">' . $header . '</h2>') : "" ?>
    <div class="row" style="margin-top: 100px; margin-bottom: 100px;">
      <div class="offset3 span6">
        <div class="alert alert-success">
          $message
        </div>
      </div>
    </div>
  </div>
}
