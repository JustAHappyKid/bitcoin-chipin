!extends 'layout.php'

block innerContent {
  <div class="container">
    <?= $header ? ('<h2 class="section_header">' . $header . '</h2>') : "" ?>
    <div class="row">
      <div class="offset3 span6">
        <div class="alert alert-success">
          $message
        </div>
      </div>
    </div>
  </div>
}
